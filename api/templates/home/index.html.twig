{% extends 'base.html.twig' %}

{% block title %}Welcome to AIctivity Planner!{% endblock %}

{% block body %}
    <div class="row align-items-center justify-content-center justify-content-lg-between">
        {% if is_granted('ROLE_USER') %}
            <div class="col-lg-6 col-md-10">
                <h1 class="header-hero-content">Hello {{ app.user.username }} ! ✅</h1>
            </div>
        {% else %}
            <div class="col-lg-6 col-md-10" >
                <div>Here is our app, please join ;)</div>
                <div>
                    <a href="{{ path('app_register') }}" class="btn btn-primary">Join</a>
                </div>
            </div>
        {% endif %}
            <div class="col-lg-4 col-md-6 col-sm-6 col-10">
                <div class="header-image">
                    <img class="img-fluid wow fadeInRightBig" src="/images/illustration.jpg" alt="Illustration Image">
                </div>
            </div>
    </div>
{% endblock %}
